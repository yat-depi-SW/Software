window.onload = function() {
    // UI/UX Design progress bar animation
    var uiuxBar = document.getElementById('uiux');
    uiuxBar.style.width = '90%';

    // Web Design progress bar animation
    var webDesignBar = document.getElementById('webdesign');
    webDesignBar.style.width = '95%';

    // HTML/CSS progress bar animation
    var htmlCssBar = document.getElementById('htmlcss');
    htmlCssBar.style.width = '80%';

    // Sketch progress bar animation
    var sketchBar = document.getElementById('sketch');
    sketchBar.style.width = '70%';
};











